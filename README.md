========= Dengo-Forum ==========

# Start the project locally

```bash


$ git clone 
$ cd Dengo-Forum
$ npm install 
$ npm install -g bower
$ npm install -g grunt-cli
$ grunt
# Open browser on http://localhost:8080
```

